const age = 23;

age >= 18 ? console.log("You're in!") : console.log('Not allowed!');

// We can also store the value of ternary operator. Example below -->

const ageCheck = 18;

const ageResult = ageCheck === 18 ? 'Adult' : 'Children';
console.log(ageResult);
